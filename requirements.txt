torch
transformers
datasets
peft
accelerate
omegaconf
evaluate
codebleu
# Keep tree-sitter packages in a CodeBLEU-compatible range.
tree-sitter==0.22.3
tree-sitter-python==0.21.0
swanlab
tqdm
pytest

# Optional acceleration packages:
# deepspeed
# flash-attn

